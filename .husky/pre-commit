# TODO: Revert back to running lint and tests in pre-commit hook
# Original code:
# npm run lint || { echo "Linting failed, run 'npm run lint:fix' to try and fix linting errors." && exit 1; }
# npm test || { echo "Tests failed" && exit 1; }

exit 0
